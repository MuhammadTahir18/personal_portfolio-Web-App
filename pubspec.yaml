name: api_flutter
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

  # ✅ Compatible with Dart 3.8.1
  google_fonts: ^6.3.2

  cupertino_icons: ^1.0.8
  dio: ^5.9.0
  logger: ^2.6.2
  flutter_svg: ^2.2.3
  url_launcher: ^6.3.2
  url_strategy: ^0.3.0
  firebase_core: ^4.4.0
  cloud_firestore: ^6.1.2


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/
